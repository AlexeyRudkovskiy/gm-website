<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">

    <link href="https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,100;0,300;0,400;0,500;0,700;0,900;1,100;1,300;1,400;1,500;1,700;1,900&display=swap" rel="stylesheet">

    {% if dashboard.pageMetaTags != null and dashboard.pageMetaTags.title is not empty %}
    <title>{{ dashboard.pageMetaTags.title }}</title>
    {% else %}
    <title>{% block title %}Welcome!{% endblock %}</title>
    {% endif %}

    {% for metaTag in dashboard.metaTags %}
        <meta name="{{ metaTag.name }}" content="{{ metaTag.content }}" />
    {% endfor %}

    <link rel="stylesheet" href="{{ asset('build/frontend.css') }}" />
</head>
<body>

    <header class="header">
        <div class="container">
            <nav class="header-navigation">
                <div class="popup-menu invisible">
                    <div class="close-icon"><a href="javascript:">X</a></div>
                    <nav class="items">
                        <span>{{'Menu'|trans}}</span>
                        <a href="#our-products">{{'Products and services' | trans}}</a>
                        <a href="#our-projects">{{ 'Our Projects' | trans }}</a>
                        <a href="#our-partners">{{ 'Our Partners' | trans }}</a>
                        <a href="#contact-us">{{'Contacts'|trans}}</a>
                    </nav>
                </div>
                <a href="javascript:" class="menu"><img src="{{ asset('assets/frontend/menu.png') }}" /></a>
                <a href="javascript:" class="logo"><img src="{{ asset('assets/frontend/logo.png') }}" /></a>
                <a href="#our-products">{{'Products and services' | trans}}</a>
                <a href="#our-projects">{{ 'Our Projects' | trans }}</a>
                <a href="#our-partners">{{ 'Our Partners' | trans }}</a>
                <a href="#contact-us">{{'Contacts'|trans}}</a>
                <div class="language-dropdown">
                    <div class="current-language">{{dashboard.currentLocale|capitalize}}</div>
                    <nav class="available-languages">
                        {% for locale in dashboard.otherLocales %}
                        <a href="{{ path('index', { _locale: locale }) }}" class="language-option">{{locale|capitalize}}</a>
                        {% endfor %}
                    </nav>
                </div>
            </nav>
            <div class="header-image-wrapper" data-images="{{ slider.photosByNames(['slider-default', 'slider-large', 'slider-small', 'slider-tablet', 'slider-mobile']) | json_encode }}">

                <picture class="header-image">
                    <source media="(min-width: 1367px)" srcset="{{ asset('uploads/photos/' ~ slider.firstPhoto.files['slider-large'].filename) }}" />
                    <source media="(min-width: 1000px) and (max-width: 1366)" srcset="{{ asset('uploads/photos/' ~ slider.firstPhoto.files['slider-default'].filename) }}" />
                    <source media="(min-width: 681px) and (max-width: 999px)" srcset="{{ asset('uploads/photos/' ~ slider.firstPhoto.files['slider-small'].filename) }}" />
                    <source media="(min-width: 400px) and (max-width: 680px)" srcset="{{ asset('uploads/photos/' ~ slider.firstPhoto.files['slider-tablet'].filename) }}" />
                    <source media="(max-width: 400px)" srcset="{{ asset('uploads/photos/' ~ slider.firstPhoto.files['slider-mobile'].filename) }}" />

                    <img src="{{ asset('uploads/photos/' ~ slider.firstPhoto.files['slider-default'].filename) }}" />
                </picture>
            </div>
        </div>
    </header>

    <div class="content-grid content-grid-hero">
        <div class="container">
            <div class="content-grid-body content-grid-body-hero">
                <div class="image hide-table hide-mobile">

                    <picture>
                        <source media="(min-width: 1367px)" srcset="{{ asset('uploads/photos/' ~ first_block.firstPhoto.files['post-default'].filename) }}" />
                        <source media="(min-width: 1000px) and (max-width: 1366)" srcset="{{ asset('uploads/photos/' ~ first_block.firstPhoto.files['post-small'].filename) }}" />
                        <source media="(min-width: 681px) and (max-width: 999px)" srcset="{{ asset('uploads/photos/' ~ first_block.firstPhoto.files['post-tablet'].filename) }}" />
                        <source media="(min-width: 400px) and (max-width: 680px)" srcset="{{ asset('uploads/photos/' ~ first_block.firstPhoto.files['post-mobile'].filename) }}" />
                        <source media="(max-width: 400px)" srcset="{{ asset('uploads/photos/' ~ first_block.firstPhoto.files['post-mobile-small'].filename) }}" />

                        <img src="{{ asset('uploads/photos/' ~ first_block.firstPhoto.files['post-default'].filename) }}" />
                    </picture>
                </div>
                <div class="content">
                    <h1 class="content-grid-header">{{first_block.title}}</h1>
                    {{ first_block.content|markdown }}
                </div>
            </div>
        </div>
    </div>

    <div class="content-grid">
        <div class="container">
            <h1 class="content-grid-header">{{second_block.title}}</h1>
            <div class="content-grid-body">
                <div class="content">
                    {{second_block.content|markdown}}
                </div>
                <div class="image">

                    <picture>
                        <source media="(min-width: 1367px)" srcset="{{ asset('uploads/photos/' ~ second_block.firstPhoto.files['post-default'].filename) }}" />
                        <source media="(min-width: 1000px) and (max-width: 1366)" srcset="{{ asset('uploads/photos/' ~ second_block.firstPhoto.files['post-small'].filename) }}" />
                        <source media="(min-width: 681px) and (max-width: 999px)" srcset="{{ asset('uploads/photos/' ~ second_block.firstPhoto.files['post-tablet'].filename) }}" />
                        <source media="(min-width: 400px) and (max-width: 680px)" srcset="{{ asset('uploads/photos/' ~ second_block.firstPhoto.files['post-mobile'].filename) }}" />
                        <source media="(max-width: 400px)" srcset="{{ asset('uploads/photos/' ~ second_block.firstPhoto.files['post-mobile-small'].filename) }}" />

                        <img src="{{ asset('uploads/photos/' ~ second_block.firstPhoto.files['post-default'].filename) }}" />
                    </picture>
                </div>
            </div>
        </div>
    </div>

    <div class="container container-offset-top-small" id="our-products">

        <h1>{{'Products and Services' | trans}}</h1>

        <div class="products-and-projects-grid products-grid">
            {% for product in products %}
                {% include 'partials/product.html.twig' with { product: product } %}
            {% endfor %}
        </div>
    </div>

    <div class="container container-offset-top" id="our-projects">

        <h1>{{'Our Projects' | trans}}</h1>

        <div class="products-and-projects-grid projects-grid">
            {% for project in projects %}
                {% include 'partials/project.html.twig' with { project: project } %}
            {% endfor %}
        </div>
    </div>

    <div class="container container-offset-top-100" id="our-partners">

        <h1>{{'Our Partners' | trans}}</h1>

        <div class="partners-grid">
            {% for partner in partners %}
                {% include 'partials/partner.html.twig' with { partner: partner } %}
            {% endfor %}
        </div>
    </div>

    <div class="container container-offset-top-100" id="contact-us">
        <h1>{{'Have questions?' | trans}}</h1>

        <div class="contact-form-container">
            <form class="contact-form" id="contact-form">
                <input type="text" class="form-input" placeholder="{{'Full Name'|trans}}" name="fullName" required />
                <input type="text" class="form-input" placeholder="{{'Phone'|trans}}" name="phoneNumber" required />
                <input type="email" class="form-input" placeholder="{{ 'E-Mail'|trans }}" name="email" required />
                <input type="text" class="form-input" placeholder="{{'Question'|trans}}" name="question" required />
                <input type="submit" class="button" value="{{'Send'|trans}}" />
            </form>
            <div class="contact-information">
                <div class="contact-information-blocks">
                    <div>
                        <p class="strong">{{ dashboard.setting('address') | nl2br }}</p>
                    </div>
                    <div>
                        <p> <strong>{{ 'Tel:' | trans }}</strong>  <a href="tel:{{ dashboard.setting('phone') }}">{{ dashboard.setting('phone') }}</a><br>
                            <strong>{{ 'Fax:' | trans }}</strong> <a href="tel:{{ dashboard.setting('fax') }}">{{ dashboard.setting('fax') }}</a><br>
                            <strong>{{ 'E-Mail:' | trans }}</strong> <a href="tel:{{ dashboard.setting('email') }}">{{ dashboard.setting('email') }}</a></p>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="map" id="map"></div>

    <footer class="footer">
        <div class="container footer-container">
            <div class="logo-section footer-section">
                <div>
                    <img src="{{ asset('assets/frontend/footer-logo.png') }}" />
                </div>
                <b class="years">2010-{{'now'|date('Y')}}</b>
                <b class="years">&copy; G&M ProcurementsKG</b>
            </div>
            <div class="products footer-section">
                <b class="footer-section-header">{{'Our Products'|trans}}</b>
                <nav class="products-list">
                    {% for product in footer_products %}
                        <a href="#product-{{ product.id }}">{{ product.title }}</a>
                    {% endfor %}
                </nav>
            </div>
            <div class="projects footer-section">
                <b class="footer-section-header">{{ 'Our Projects' | trans }}</b>
                {{ dashboard.setting('translations.'~app.request.locale~'.projects')|markdown }}
            </div>
            <div class="subscribe footer-section">
                <div class="visible-only-1000 margin-top">
                    <b class="years">2010-{{'now'|date('Y')}}</b><br>
                    <b class="years">&copy; G&M ProcurementsKG</b>
                </div>
            </div>
        </div>
    </footer>

    <div class="popup-container hidden">
        <div class="popup-content">
            <div class="popup-header">
                <img src="{{ asset('assets/temp/popup.png') }}" />
                <div class="close-button">
                    <img src="{{ asset('assets/frontend/close.png') }}" />
                </div>
            </div>
            <div class="content"></div>
        </div>
    </div>

    {% block body %}{% endblock %}

    {{ encore_entry_script_tags('frontend-js') }}
    <script src="https://maps.googleapis.com/maps/api/js?key={{ google_maps_api_key }}&callback=initMap" async defer></script>

    {% block javascripts %}{% endblock %}
</body>
</html>
