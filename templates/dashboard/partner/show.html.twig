{% extends 'admin.html.twig' %}

{% block title %}Partner{% endblock %}

{% block body %}
    <h1>Partner</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ partner.id }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ partner.image ? partner.image|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ partner.title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('partner_index') }}">back to list</a>

    <a href="{{ path('partner_edit', {'id': partner.id}) }}">edit</a>

    {{ include('dashboard/partner/_delete_form.html.twig') }}
{% endblock %}
