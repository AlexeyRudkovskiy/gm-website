<form method="post" action="{{ path('page_meta_tags_delete', {'id': page_meta_tag.id}) }}" onsubmit="return confirm('{{ 'Are you sure you want to delete this item?'|trans }}');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ page_meta_tag.id) }}">
    <div class="vertical-spacer"></div>
    <button class="button">{{'Delete' | trans}}</button>
</form>
